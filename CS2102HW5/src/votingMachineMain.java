// Run VotingMachine IO by running main class
public class votingMachineMain {
    public static void main(String[] args) {
        // Create an instance of VotingMachine
        VotingMachine votingMachine = new VotingMachine();

        // Call the run method to start the voting machine
        votingMachine.run();
    }
}